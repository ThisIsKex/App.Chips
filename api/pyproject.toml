[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
]

[dependency-groups]
dev = [
    "aws-lambda-powertools>=3.8.0",
    "boto3>=1.37.17",
    "ruff>=0.11.1",
]
